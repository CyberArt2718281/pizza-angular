# 📚 ANGULAR - КОНСПЕКТ ДЛЯ НОВИЧКА

## 📋 Содержание

1. [Передача данных между компонентами](#1-передача-данных-между-компонентами)
2. [Доступ к элементам и компонентам](#2-доступ-к-элементам-и-компонентам)
3. [Контентная проекция](#3-контентная-проекция-ng-content)
4. [Инкапсуляция стилей](#4-инкапсуляция-стилей)
5. [Жизненный цикл компонента](#5-жизненный-цикл-компонента)
6. [Дополнительные возможности](#6-дополнительные-возможности)
7. [Практические советы](#практические-советы)

---

## 1. ПЕРЕДАЧА ДАННЫХ МЕЖДУ КОМПОНЕНТАМИ

### 1.1 От Родителя к Ребёнку (`@Input`)

> **Что это:** Как папа даёт конфетку ребёнку 🍭

**Как работает:**

```typescript
// В дочернем компоненте (ребёнок получает данные)
@Input() productName: string = '';
@Input() price: number = 0;
```

```html
<!-- В родительском шаблоне (папа передаёт данные) -->
<product-card [productName]="'Пицца Маргарита'" [price]="500"> </product-card>
```

**💡 Простыми словами:** Родительский компонент "говорит" дочернему: "Вот тебе название пиццы и цена, покажи их пользователю"

### 1.2 От Ребёнка к Родителю (`@Output`)

> **Что это:** Как ребёнок кричит маме: "Мама, я хочу эту игрушку!" 📢

**Как работает:**

```typescript
// В дочернем компоненте (ребёнок отправляет сигнал)
@Output() addToCart = new EventEmitter<string>();

onClick() {
  this.addToCart.emit('Пицца добавлена!'); // Кричим родителю
}
```

```html
<!-- В родительском шаблоне (мама слушает) -->
<product-card (addToCart)="handleCartAdd($event)"></product-card>
```

```typescript
// В родительском компоненте
handleCartAdd(message: string) {
  alert(message); // Мама услышала и отреагировала
}
```

**💡 Простыми словами:** Дочерний компонент "кричит" родительскому: "Эй, пользователь нажал кнопку, сделай что-нибудь!"

### 1.3 EventEmitter - "Рупор" для компонентов

> **Что это:** Как рация или мегафон 📻

**Как работает:**

- **Создаём "рацию":** `new EventEmitter()`
- **"Кричим в рацию":** `.emit('сообщение')`
- **"Слушаем рацию":** `(событие)="метод($event)"`

**🍕 Пример из жизни:** Как в пиццерии повар кричит "Заказ готов!" и официант это слышит

---

## 2. ДОСТУП К ЭЛЕМЕНТАМ И КОМПОНЕНТАМ

### 2.1 Template Reference Variables (Локальные переменные)

> **Что это:** Как дать прозвище элементу в HTML 🏷️

```html
<input #phoneInput type="text" /> <button (click)="getValue(phoneInput.value)">Получить значение</button>
```

**💡 Простыми словами:** Мы говорим: "Этот input теперь называется `phoneInput`, и я могу к нему обращаться по имени"

### 2.2 `@ViewChild` - "Найди мне этот элемент"

> **Что это:** Как попросить Angular: "Найди мне тот синий div и дай мне с ним работать" 🔍

```typescript
@ViewChild('phoneInput') phoneRef!: ElementRef;

ngAfterViewInit() {
  console.log(this.phoneRef.nativeElement.value);
  // Теперь можем управлять из TypeScript
}
```

⚠️ **Важно:** Обращаться к `@ViewChild` только в `ngAfterViewInit` или позже!

### 2.3 `@ContentChild` - "Найди то, что мне подсунули"

> **Что это:** Когда родитель засовывает что-то в ваш компонент, и вы хотите это найти 🎁

```html
<!-- Родитель засовывает кнопку в наш компонент -->
<my-card>
  <button #cardButton>Нажми меня</button>
</my-card>
```

```typescript
// Наш компонент может найти эту кнопку
@ContentChild('cardButton') button!: ElementRef;
```

---

## 3. КОНТЕНТНАЯ ПРОЕКЦИЯ (`<ng-content>`)

### Что это: "Окошко" в компоненте

> **Как окно в стене - родитель может засунуть туда что угодно** 🪟

```html
<!-- В дочернем компоненте делаем "окошко" -->
<div class="card">
  <h2>Заголовок карточки</h2>
  <ng-content></ng-content>
  <!-- Сюда родитель засунет что захочет -->
</div>
```

```html
<!-- Родитель использует наш компонент -->
<card-component>
  <p>Этот текст появится в окошке!</p>
  <button>И эта кнопка тоже!</button>
</card-component>
```

### Селективная проекция (с `select`)

```html
<div class="card">
  <ng-content select="[slot=header]"></ng-content>
  <!-- Только для header -->
  <ng-content select="[slot=body]"></ng-content>
  <!-- Только для body -->
</div>
```

```html
<!-- Использование -->
<card-component>
  <h1 slot="header">Заголовок</h1>
  <p slot="body">Содержимое</p>
</card-component>
```

---

## 4. ИНКАПСУЛЯЦИЯ СТИЛЕЙ

### Что это: "Невидимый забор" вокруг стилей

> **По умолчанию стили компонента не убегают наружу и чужие не попадают внутрь** 🏠

```scss
/* В компоненте product.component.scss */
.title {
  color: red; /* Покрасит только заголовки ВНУТРИ этого компонента */
}
```

### Режимы `ViewEncapsulation`:

| Режим                     | Описание          | Аналогия                                 |
| ------------------------- | ----------------- | ---------------------------------------- |
| `Emulated` (по умолчанию) | Стили изолированы | "Забор есть, но не очень высокий" 🚧     |
| `ShadowDom`               | Полная изоляция   | "Железобетонный забор" 🏰                |
| `None`                    | Никакой изоляции  | "Никакого забора, стили летают везде" 🌪️ |

---

## 5. ЖИЗНЕННЫЙ ЦИКЛ КОМПОНЕНТА

### Что это: Этапы жизни компонента

> **Как у человека: рождение, взросление, смерть** 👶➡️👨➡️💀

### 5.1 `ngOnInit` - "Компонент родился"

```typescript
ngOnInit() {
  console.log('Привет, я родился! 👶');
  // Здесь делаем начальную настройку
  this.loadUserData();
}
```

**⏰ Когда:** Один раз при создании компонента

### 5.2 `ngOnChanges` - "Родители дали новые данные"

```typescript
ngOnChanges(changes: SimpleChanges) {
  console.log('Родители изменили мои @Input свойства! 📦');
  if (changes['userName']) {
    this.updateGreeting();
  }
}
```

**⏰ Когда:** Каждый раз, когда меняются `@Input` свойства

### 5.3 `ngAfterViewInit` - "Я полностью нарисовался"

```typescript
ngAfterViewInit() {
  console.log('Теперь я вижу все свои HTML элементы! 👀');
  // Здесь можно работать с @ViewChild элементами
  this.focusOnInput();
}
```

**⏰ Когда:** Когда компонент и все его дети полностью отрисованы

### 5.4 `ngOnDestroy` - "Меня сейчас удалят"

```typescript
ngOnDestroy() {
  console.log('Пока-пока, меня удаляют! 👋');
  // Здесь убираем за собой мусор
  this.subscription.unsubscribe();
}
```

**⏰ Когда:** Перед удалением компонента

### 5.5 Остальные хуки (для продвинутых):

| Хук                     | Когда срабатывает             | Зачем нужен                        |
| ----------------------- | ----------------------------- | ---------------------------------- |
| `ngDoCheck`             | При каждой проверке изменений | "Проверяю изменения сам" 🕵️        |
| `ngAfterContentInit`    | После вставки контента        | "Контент от родителя вставился" 📥 |
| `ngAfterContentChecked` | После проверки контента       | "Проверил контент от родителя" ✅  |
| `ngAfterViewChecked`    | После проверки представления  | "Проверил свой вид" 🪞             |

---

## 6. ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ

### 6.1 Геттеры и Сеттеры для `@Input`

```typescript
private _userName: string = '';

@Input()
get userName(): string {
  return this._userName;
}

set userName(value: string) {
  this._userName = value.toUpperCase(); // Автоматически делаем заглавными
  this.updateDisplay();
}
```

### 6.2 Двустороннее связывание (создаём свой `ngModel`)

```typescript
// Создаём свой ngModel
@Input() value: string = '';
@Output() valueChange = new EventEmitter<string>();

updateValue(newValue: string) {
  this.value = newValue;
  this.valueChange.emit(newValue); // Уведомляем родителя
}
```

```html
<!-- Использование -->
<my-input [(value)]="userName"></my-input>
```

### 6.3 TrackBy для оптимизации `*ngFor`

```typescript
trackByProduct(index: number, product: Product): string {
  return product.title + product.image; // Уникальный идентификатор
}
```

```html
<div *ngFor="let product of products; trackBy: trackByProduct">
  <!-- Теперь Angular не будет пересоздавать DOM без необходимости -->
</div>
```

---

## 📝 ПРАКТИЧЕСКИЕ СОВЕТЫ

| Ситуация             | Используй     | Пример                                 |
| -------------------- | ------------- | -------------------------------------- |
| Передать данные вниз | `@Input`      | Название товара от каталога к карточке |
| Сообщить наверх      | `@Output`     | Клик по кнопке "Купить"                |
| Управлять элементом  | `@ViewChild`  | Программно сфокусироваться на input    |
| Начальная настройка  | `ngOnInit`    | Загрузить данные пользователя          |
| Очистка ресурсов     | `ngOnDestroy` | Отписаться от подписок                 |
| Гибкий компонент     | `ng-content`  | Универсальная карточка                 |

---

## ❌ ТИПИЧНЫЕ ОШИБКИ НОВИЧКОВ

### 1. Забыть импортировать `FormsModule` для `ngModel`

```typescript
// app.module.ts
import { FormsModule } from '@angular/forms';

@NgModule({
  imports: [FormsModule], // Не забудь!
})
```

### 2. Обращаться к `@ViewChild` в `ngOnInit`

```typescript
❌ ngOnInit() {
  this.myElement.nativeElement.focus(); // Элемента ещё нет!
}

✅ ngAfterViewInit() {
  this.myElement.nativeElement.focus(); // Теперь есть!
}
```

### 3. Забыть отписаться в `ngOnDestroy`

```typescript
❌ // Подписка будет висеть в памяти

✅ ngOnDestroy() {
  this.subscription.unsubscribe(); // Убираем за собой
}
```

### 4. Мутировать `@Input` данные напрямую

```typescript
❌ this.inputData.push(newItem); // Изменяем исходный объект

✅ this.inputData = [...this.inputData, newItem]; // Создаём новый
```

### 5. Неправильный импорт `EventEmitter`

```typescript
❌ import { EventEmitter } from 'events'; // Node.js версия

✅ import { EventEmitter } from '@angular/core'; // Angular версия
```

---

## 🎯 ШПАРГАЛКА ДЛЯ БЫСТРОГО ПОИСКА

### Синтаксис привязки данных:

- `[property]="value"` - односторонняя привязка (в элемент)
- `(event)="handler()"` - обработка событий (из элемента)
- `[(ngModel)]="property"` - двусторонняя привязка
- `{{value}}` - интерполяция (вставка значения)

### Декораторы:

- `@Input()` - получение данных от родителя
- `@Output()` - отправка событий родителю
- `@ViewChild()` - доступ к дочернему элементу/компоненту
- `@ContentChild()` - доступ к проецируемому контенту

### Жизненный цикл (порядок):

1. `ngOnChanges` (при изменении @Input)
2. `ngOnInit` (однократно при создании)
3. `ngAfterContentInit` (контент загружен)
4. `ngAfterViewInit` (представление готово)
5. `ngOnDestroy` (перед удалением)

---

**🚀 Удачи в изучении Angular! Этот конспект поможет тебе быстро найти нужную информацию.**
